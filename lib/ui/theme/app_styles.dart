import 'package:flutter/material.dart';

class AppColors {
  // Primary background colors
  static const Color darkBackground = Color(0xFF0f172a);
  static const Color secondaryBackground = Color(0xFF1e293b);
  static const Color tertiaryBackground = Color(0xFF334155);

  // Accent colors
  static const Color primaryBlue = Color(0xFF1e40af);
  static const Color accentBlue = Colors.blue;

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textTertiary = Colors.grey;

  // Utility colors
  static const Color disabled = Color(0xFF374151);

  static const Color cardBackground = Color(0xFF1e293b);
  static const Color cardBackgroundGradientStart = Color(0xFF1e293b);
  static const Color cardBackgroundGradientEnd = Color(0xFF0f172a);
  static const Color cardBorder = Color(0xFF334155);

  static const Color buttonBackground = Color.fromARGB(255, 50, 62, 80); // slate-700
  static const Color buttonBackgroundHover = Color(0xFF475569); // slate-600
  static const Color buttonBorder = Color(
    0xFF475569,
  ); 
  static const Color buttonText = Color(0xFFD1D5DB); // gray-300
  static const Color buttonTextHover = Colors.white;

  AppColors._();
}
